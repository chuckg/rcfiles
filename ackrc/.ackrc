# ack will not search .thor files (which is ruby) by default, so we have to add it as perl. wtf?
--type-add
perl=.thor

--type-add
css=.scss

--type-add
html=.haml

--type-add
ruby=.haml

--type-add
js=.coffee
